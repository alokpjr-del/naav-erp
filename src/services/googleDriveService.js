// Google Drive / OAuth integration has been removed.
// Backup system operates strictly on local JSON storage and optional S3/R2 cloud storage.

module.exports = {
    disabled: true,
    message: 'Google Drive backup integration has been removed from this application.'
};
